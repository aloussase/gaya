true  :: 1
false :: 0

(*
  Return the first element in a 2-element array.
  @param xs <Array> The array.
*)
fst :: { (x, _) => x }

(*
  Return the second element in a 2-element array.
  @param xs <Array> The array.
*)
snd :: { (_, x) => x }
