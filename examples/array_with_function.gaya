(* Output: 'Inside array!\nGaya' *)

f :: { name =>
  { => io.println(name) }
}

discard 
  let xs = ({ => io.println("Inside array!") }) in
  let ys = ( f("Gaya") ) in do
    discard xs(0)()
    ys(0)()
  end
