(* Output: '69\n42' *)

max :: { x, y =>
  cases
    given x > y => x
    otherwise   => y
  end
}

min :: { x, y =>
  cases
    given x < y => x
    otherwise   => y
  end
}

io.println(max(42, 69)).

io.println(min(42, 69)).
