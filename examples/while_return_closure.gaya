(* Output: 'i' *)

test :: { =>
  let f = unit in
  let run = 1 in do
    while run do
      discard let i = "i" in do (* TODO: Replace with ignore *)
        f <- { => io.println(i) }
        unit
      done
      run <- 0
    done

    f
  done
}

h :: test()

discard h()
