(* Output: '<function-0>\n69' *)

include "functions"

f :: { x =>
  cases
    otherwise =>
      let y = 42 in
      { => fn.add(x, y) }
  end
}

discard io.println(f(""))

discard io.println(f(27)())
