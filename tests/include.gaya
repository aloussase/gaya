include "./file_to_be_included.gaya"
include "functions"
include "file_to_be_included_without_extension"

someFunction()      |> assert(_ == "From included").
fn.add(1, 2)        |> assert(_ == 3).
someOtherFunction() |> assert(_ == "From other included").
