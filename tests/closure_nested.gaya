(* Output: 'a\nb\nc' *)

f :: { =>
  let a = "a" in
  { =>
    let b = "b" in
    { =>
      let c = "c" in do
        io.println(a).
        io.println(b).
        io.println(c)
      end
    }
  }
}

f()()().
