(* Output: 'a\nb\nc' *)

f :: { =>
  let a = "a" in
  { =>
    let b = "b" in
    { =>
      let c = "c" in do
        discard io.println(a)
        discard io.println(b)
        io.println(c)
      end
    }
  }
}

discard f()()()
