(* Output: 'anon says: Hello!\nanon says: Hallo!' *)

include "strings"

test :: { s =>
  s
  |> string.trim(_)
  |> string.concat(_, "!")
  |> string.concat("anon says: ", _)
}

io.println(test("   Hello \n ")).
io.println(test("   Hallo   ")).
