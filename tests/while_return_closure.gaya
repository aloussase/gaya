(* Output: 'i' *)

test :: { =>
  let f = unit in
  let finished = 0 in do
    while not finished
      discard let i = "i" in perform f <- { => io.println(i) }
      finished <- 1
    end

    f
  end
}

h :: test()

discard h()
