(* Output: 'number\nstring\nunit\nOops' *)

io.println(typeof(42)).

io.println(typeof("Hello")).

io.println(typeof(unit)).

let result = 69 / 0 in
cases
  given typeof(result) == "unit" => io.println("Oops")
  otherwise                      => io.println(result)
end.
